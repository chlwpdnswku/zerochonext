const initalState = {
  name: '오광민',
  age: 69,
  password: '공부안함',
};

const rootReducer = (state = initalState, action) => {
  switch (action.type) {
    case 'CHANGE_NICKNAME':
  }
};

export default rootReducer;
